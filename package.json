{
  "name": "network-simulator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:game": "pnpm --filter @network-simulator/game dev",
    "dev:editor": "pnpm --filter @network-simulator/editor dev",
    "build:game": "pnpm --filter @network-simulator/game build",
    "build:editor": "pnpm --filter @network-simulator/editor build",
    "lint": "pnpm -r lint",
    "test:shared": "pnpm --filter @network-simulator/shared test",
    "test": "pnpm -r test"
  }
}
